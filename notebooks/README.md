# Notebooks

This directory is reserved for exploratory analysis only.

## Rules

- Notebooks are optional and should not become the source of truth for the system.
- If an analysis matters for project understanding, promote the conclusion into `docs/project/` or `docs/research/`.
- If notebook logic becomes reusable, move it into `src/typhon/` or `scripts/`.
